I have a Python file with the following issues from a code review:

1. Security (high): The `language` parameter is directly interpolated into a regex pattern without escaping, causing ReDoS risk: `rf"```{language}\n(.*?)```"` - should use `re.escape(language)`

2. Bug (high): Direct index access `resp["choices"][0]` assumes API always returns at least one choice. Should guard with `len(resp.get("choices", [])) > 0`

3. Bug (high): Deep dictionary access will raise KeyError/TypeError. Use nested .get() calls: `resp.get("choices", [{}])[0].get("message", {}).get("content", "")`

4. Bug (high): `content or reasoning or None` uses truthiness on strings - if content="0" it's falsy. Use explicit `if/else` checks.

5. Bug (medium): `json.loads()` called without validation, catching all Exceptions masks tracebacks. Catch `json.JSONDecodeError` specifically.

Please read the file at /home/fource/bytecats/projects/web/easycv/automation/llm_client.py and provide the fixed code addressing all these issues. Return only the complete fixed file content.