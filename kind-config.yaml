# kind cluster config — maps host ports to the cluster for local testing
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      # Frontend: host 8080 → cluster NodePort 3000
      - containerPort: 30000
        hostPort: 8080
        protocol: TCP
